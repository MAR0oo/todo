import { FirstLetterPipePipe } from './first-letter-pipe.pipe';

describe('FirstLetterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLetterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
